{
  "format": 1,
  "description": "Khôi phục vùng bấm trang bị bằng cách không đưa toàn bộ bảng chỉ số lên trên các ô trang bị.",
  "files": [
    {
      "path": "scripts/view/game/property/page/PropertyRoleEquipPage.as",
      "replacements": [
        {
          "source": "if(this.contains(_otherEquipGrid))\n         {\n            this.removeChild(_otherEquipGrid);\n            this.addChild(_otherEquipGrid);\n         }\n         this.addChild(this._propertyPanel);\n         this._wingBtn = new MsgButton(MsgButtonTypes.Wing);",
          "target": "if(this.contains(_otherEquipGrid))\n         {\n            this.removeChild(_otherEquipGrid);\n            this.addChild(_otherEquipGrid);\n         }\n         this._wingBtn = new MsgButton(MsgButtonTypes.Wing);",
          "expected": 1
        }
      ]
    }
  ]
}
